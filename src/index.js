import React from 'react'
import ReactDOM from 'react-dom'

import "./assets/scss/App.scss"

const wrapper = document.getElementById("app")
wrapper && ReactDOM.render(<h1>Conway's Game of Life"</h1>, wrapper)
